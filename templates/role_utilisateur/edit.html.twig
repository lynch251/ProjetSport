{% extends 'base.html.twig' %}

{% block title %}Edit RoleUtilisateur{% endblock %}

{% block body %}
    {% block headerUser %}
        {% import 'macro/macroHeader.html.twig' as headerUser %}
        {{ headerUser.headerUser('test',1) }}
    {% endblock %}
<div class="container">
    <div class="row">
        <div class="col-lg-12 col-xl-10 mx-auto mt-5 mb-5">
            <h1>Modifier le rôle utilisateur : {{ role_utilisateur.titre }}</h1>
            {{ include('role_utilisateur/_form.html.twig', {'button_label': 'Update'}) }}

            <a class="btn btn-primary mb-3 mt-3" href="{{ path('role_utilisateur_index') }}" role="button">Retour à la liste</a>

            {{ include('role_utilisateur/_delete_form.html.twig') }}
        </div>
    </div>
</div>

{% endblock %}
