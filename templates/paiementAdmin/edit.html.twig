{% extends 'base.html.twig' %}

{% block title %}Edit Paiement{% endblock %}

{% block body %}
    {% block headerUser %}
        {% import 'macro/macroHeader.html.twig' as headerUser %}
        {{ headerUser.headerUser('test',1) }}
    {% endblock %}
    <h1>Edit Paiement</h1>

    {{ include('paiementAdmin/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('paiementAdmin_index') }}">back to list</a>

    {{ include('paiementAdmin/_delete_form.html.twig') }}
{% endblock %}
